# TABsucks development, test, and Windows build dependencies
#
# Install this after choosing either requirements.txt (CPU) or
# requirements-gpu.txt (NVIDIA GPU).

# Tests
pytest>=9,<10
pytest-cov>=4,<8
pytest-asyncio>=0.23,<2
httpx>=0.28,<0.29

# Code quality
ruff>=0.15,<0.16
black>=24,<27
isort>=5.13,<7
mypy>=1.16,<2
pre-commit>=3,<5

# Windows distribution
pyinstaller>=6.21,<7
