# TABsucks default source runtime (CPU inference)
#
# Intended for 64-bit Python 3.10. The CPU dependency set has not yet been
# validated in a clean Windows environment.

-r requirements-base.txt

# Explicit CPU PyTorch wheels. Using the local version suffix prevents pip
# from silently choosing a CUDA build.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu
torchaudio==2.6.0+cpu

# audio-separator does not install an ONNX backend unless an extra is chosen.
audio-separator[cpu]>=0.44,<0.45
onnxruntime>=1.17,<2
